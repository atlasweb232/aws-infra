# Terraform state files (use remote state for production)
*.tfstate
*.tfstate.backup

# Terraform directory
.terraform/

# SSH key (keep this private)
atlas-keypair.pem

# OS files
.DS_Store
Thumbs.db
